# azya52 Import

Imported from:

`C:\Users\szept\OneDrive\Dokumentumok\!Magán\!Technikai dolgok\Seiko UC-2000\azya52`

This appears to be a local copy of the `azya52/seiko` reverse-engineering project for the Seiko UC-2000 / UC-2200 family.

## Copied Material

- `README.md` copied to `docs/notes/azya52-README.md`.
- `docs/` copied to `docs/original/azya52/docs/`.
- `ads/`, `decap/`, and `misc/` copied to `docs/original/azya52/`.
- `apps/` copied to `examples/assembly/azya52/apps/`.
- `rom/*.bin` copied to `examples/machine-code/azya52/rom/`.
- `rom/disassembled/` copied to `examples/assembly/azya52/rom-disassembled/`.
- App `.bin` files copied to `examples/machine-code/azya52/apps/`.
- `axasm/` copied to `tools/assembler/axasm/`.
- `ucDisassembler/` copied to `tools/disassembler/ucDisassembler/`.
- `Transmitters/` copied to `tools/transmitter/azya52/`.
- `UC2000Keyboard/` copied to `tools/keyboard-emulator/UC2000Keyboard/`.
- `firmware/` copied to `tools/firmware/azya52/`.

## Notes

- The original `.git` directory was not copied.
- Build outputs and executables from the source folders were preserved when they were part of the copied tool directories.
- Technical claims from these files should still be traced to source documents or tested on hardware before being added to the Codex skill references.
